class MonteCarloPi {
	public int getDouble(int a, int b) {
		int c;
		int d;
		int[] x;
		int[] y;

		x = new int[2];

		x[0] = 1;
		x[1] = 2;

		// y = x;

		c = x[0];

		c = 10 * x.length + x[0];

		return x.length;
	}


	public static void main(String[] args) {
		int num_samples;
		int x;

		x = 10;

		if(16 < 5) {
			num_samples = 2;
		} else {
			num_samples = 5;
			num_samples = num_samples * x;
		}
	}
}